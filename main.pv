fn build(compile_io: IO, compiler: Compiler) {
    compiler.executable("main", () => ma);
    compiler.library("lib", () => )
}

fn main(compile_io: IO, io: IO) {
    let text = read_file(compile_io, "test.txt");

    print(io, text);
}

fn library_function()