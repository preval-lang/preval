compile_io.println("Running at build time");

compiler.executable("program", (io: IO) => {
    io.println("Hello, World!");
});

let print_hello_world = (io: IO) => {
    io.println("Hello, World!");
};

compiler.library("helloworld", {
    print_hello_world,
});

// preval main.pv
// this will compile main.pv, link it with the compiler library, and run it
// running it will produce the output program